# Frontend environment (Vite).
# In local dev you can leave this blank — Vite proxies /api to http://localhost:4000.
# For production (Vercel), set this to your deployed backend origin, e.g.
#   VITE_API_URL=https://mkt-studio-api.onrender.com
VITE_API_URL=
